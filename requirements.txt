# Pinned to the locally verified stack (2026-07-10). Streamlit Cloud installs
# whatever resolves at deploy time; open ranges previously pulled pandas 3.x /
# yfinance 1.5.x — a dependency matrix the app was never tested on.
streamlit==1.54.0
requests==2.32.5
yfinance==1.1.0
pandas==2.3.3
numpy==2.4.2
altair==6.0.0
streamlit-searchbox==0.1.24
